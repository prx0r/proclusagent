# docs/stale/ — candidates, not moves: NOTHING MOVED this turn

Alpha: the 14-doc canon (README index + SYNTHESIS/KERNELS/HANDOVER/AUDIT/…) is current;
staleness risk is duplication between HANDOVER (session log) and QUEUE/THREADS (live state).
Rule: HANDOVER is append-only history, QUEUE/THREADS are live — if they disagree, live wins,
history gets a correction note, never a rewrite. Future quarantine candidates (H3 cover):
superseded RUN-RECEIPTs if digests rotate. Nothing deleted, no links broken.
