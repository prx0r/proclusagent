# FILES — manifest (tracked + intentional untracked)

- `AGENTS.md`, `README.md`, `.env.example`, `.gitignore`(upstream)
- `docs/` — README, RECIPES, FILES, THREADS, HANDOVER, SYNTHESIS, KERNELS,
  MINE-SPEC, LINEAGE-MAP, MINI-ME, MCP-PLAN, CMAIL-THESIS, AUDIT, LOOP, QUEUE,
  A-QUEUE.jsonl, A-NOTES.md, R2-OPENCODE, R2-INVENTORY, ROADMAP, RUN-RECEIPT-*;
  `docs/stale/` manifests (nothing moved)
- `schemas/` — verse-claim.json, arch-packet.json; `schemas/stale/` manifest
- `examples/` — prop-01.md; `examples/stale/` manifest
- `tests/` — test_boot.py (keeps `tests-exist` green)
- `aloop.py` — recursive A-task loop
- `runs/` — content-addressed receipts (evidence, keep)
- `stale/` — top-level scratch manifest (A-NOTES lives here by convention soon)
- Linked (not vendored): `/home/ubuntu/seed0/mine/architectures/` (57 packets, 31 boot_tests)
