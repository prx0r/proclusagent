# HANDOVER 2026-09-10T19:46Z — full session, timestamped (main project + data hunt)

## Where everything stands
- proclusagent @ `chain/session-logs-1` (pushed fc9e0f2; NEW uncommitted work below).
  Suite 95+2skip, checker 5/5. a-logs 116 records.
- seed0 @ `chain/ham-tournament-1` (pushed 7c5ca67). Upstream origin/main +392 files
  fetched, NOT merged (needs human call). Mine: 57 arch packets, seeds incl. 6-ham.
- /tmp quarantine (NOT repo): work.db 6GB + box2.db 2.1GB, corpora (8,497 + 4,915
  sends, scrubbed), WAL/headers/tails, mininet venv, replay stores, R2 creds ENV-ONLY.

## Mini-me track (main project) — best numbers
- fastText text→family **0.807** (0.799 unseen) · fam-hit@3 0.671 · hashed-linear
  ctx→family 0.437 · session-prior 0.436 online / 0.02 frozen · kNN 2nd everywhere ·
  MLP loses (0.38) · exact-template 0.0016 (dead — proves buttons).
- **CROSS-BOX (both directions, new)**: blend (0.4,0.4,0.2) wins cat BOTH ways
  (0.4232 box1→2, 0.4083 box2→1); session-prior collapses cross-box (0.015) as
  predicted; kNN transfers. Raw: /tmp/cross_12.json, /tmp/cross_21.json.
- Continue auto: FALSIFIED three ways (history 0.195, dialog-act 0.166, joint
  0.193); gate CLOSED by measurement; dialog-state path documented.
- Replay sim: exact-text scoring never learns (flat); family ranker loses
  (0.52<0.64, text default retained); slice-conditional deferral queued (L2D).
- Shipped live: `p`/`pt` shell fns, `/predict` command, MCP (real protocol, 8 tools,
  enabled), CLI (suggest+chains+type-tags), predictor/ (14 modules), controller/,
  ham_registry.py, aloop.py (34+ done + full_test/analyze/amend/report/verify),
  prompt library (27 specimens → 10 categories), TEN-KEYS instrument.
- Theory recorded: INSTRUMENT-THEORY.md (DirectGPT/Log2Plan/OS-Kairos/AGORA/
  CogniGUI + Beaudouin-Lafon counter + 3 build orders).

## Data track — inventory (all /tmp, never pushed)
- FI-2010 940MB verified (149-row layout proven) + MSc 52MB variant + GSK LSE
  726,666 rows (events proven exact: trade-join 100%, orphans 0, seq 99.99%;
  book-state FALSIFIED: 75% crossed, 1107bps spreads) + adapter + known-answer
  tests + LSE.txt universe. fishdata repo live (manifests/schemas/adapter only).
- 2018 MBO fingerprints: partial (2× 403, schema hits only). ITCH/LSEG/KRX queued free.

## Open gates (human decisions, in order)
H: UI placement for buttons · `merge seed0` · main-branch merge · rotate exposed
ghp token (STILL OPEN — in chat history + R2 WAL) · LOBSTER/Cboe/HF/Kaggle approvals ·
Databento manual pull. M: deny all ($0 spent).

## Resume commands (fresh agent)
`cd /home/ubuntu/proclusagent && python3 -m pytest tests/ -q && python3 ../seed0/seed0.py check .`
`python3 aloop.py --status` · `python3 aloop.py --until-blocked --max 8 --evolve-loop`
Full method: docs/METAGUIDE.md · product status: docs/BUTTONS-REPORT.md ·
numbers: docs/EXPERIMENT-REF.md · this session's log: docs/RUN-LOG-2026-09-10-ALL-A.md.
