# SYNTHESIS — proclusagent as executable architecture tournament (2026-09-10)

Status: proclusagent is EMPTY → treat as new synthesis repo, not another archive.
Question is no longer "can philosophy inspire an agent?" but:
**Which claimed structures of cognition/reality, instantiated as executable
information-processing architectures, actually produce measurably better agents?**

## 1. Donor mechanisms (lift, don't admire)

- **knowledge-base-organism → ontological substrate.** General epistemic graph:
  claim / argument / evidence / review / immutable-artifact; compilation into
  immutable read plane; agent bundles/MCP; 52 kernels; 97 experiments. Doctrine:
  output is real only with reproducible inputs + outputs + explicit epistemic ceiling.
  In proclusagent: every Proclean entity, proposition, causal relation, hypothesis,
  observation, agent conclusion = typed graph node/edge, never prose-in-context.
- **cogym → scientific harness.** Freeze hypothesis → inject reasoning strategy →
  run on deterministic worlds → grade vs oracle → peer-review → next experiment.
  Core rule: "Hermes proposes. Cogym disposes. No LLM grades itself." Lets us run
  ProclusKernel / TantraKernel / NāṇavīraKernel / ActiveInferenceKernel / BaselineReAct
  on identical tasks and measure whether metaphysics helps reasoning.
- **seed0 → outer tournament.** Five kernels already (minimal, spec-first,
  evidence-maximalist, multi-agent lanes, red-team-first) + mechanical scorer
  (compliance + suite + evidence). proclusagent starts as ONE SEED among many;
  mutate Proclean primitives independently, keep what survives.
- **QDW → verification constitution.** V0→V12 ladder: compile/static/unit →
  property/concurrency/adversarial → factory fixtures/Docker/E2E/live/CI. Forbids
  "done because code exists." Lift wholesale: every claimed advantage gets a
  certificate (IMPLEMENTED_UNVERIFIED / PROVEN-MECHANISM / PROVEN).
- **Alethiea → dependency-aware beliefs.** Records observation→conclusion deps,
  detects upstream change, computes blast radius, emits targeted proof obligations
  instead of recompute-all. Maps to remaining→procession→reversion as incremental
  reasoning: change one assumption → re-evaluate only dependency descendants.
- **patalacheckpoints → evidence-bearing relations.** Rejects naive parent hierarchy
  for typed relations with confidence + evidence; API/data graph canonical, UI a
  projection. Proclean hierarchy becomes typed edges (grounds, participates_in,
  produces, returns_to, contains_as_cause, analogous_to, contradicts) with
  provenance + confidence — not One > Intellect > Soul as static tree.
- **Iolaus / MemoryProof → memory controls.** Factual/multi-hop/contradiction/
  temporal/negative questions vs ground truth; classifies retrieval failures;
  optimizes memory configs. Separates architecture advantage from retrieval advantage
  (easiest way hierarchical agents fool us is better structured memory).
- **hxrmxs-truth-engine → interpretation branches.** AM0 keeps ontologically neutral
  spine, branches physical-realist / Platonic-computational / nondual / process /
  others, each with evidence clusters + falsifiers + confidence. Core mechanism:
  never collapse ontology prematurely; propagate evidence separately per branch.
  Pipeline shape: question → truth map → branches → evidence → falsifiers →
  confidence update (CODEX-TRUTHMAP, EVIDENCE-PIPELINE, COMPOSITION-FORMAT,
  AM0-framework, DEEP-MATH-ALGORITHMS).
- **research-journal → nested coordination.** Beyond diary: metaphysics, tractatus0,
  pwan grounding, pwan levin threads. pwan levin bridge: high-level pattern
  coordinates lowers via "cognitive glue"; selves nest; success = lowers aligning
  to higher goal. Experiment: hierarchy transmitting constraints/goals (not token
  instructions) vs flat supervisor.
- **EvoLabz → mundane control.** Sensors → Modules → Strategies (atomic observations
  → pattern detectors → integrated decisions). Old trading code, same compositional
  shape — the non-metaphysical control to beat.

## 2. Architecture (every term = falsifiable op)

```
                 THE ONE / GLOBAL OBJECTIVE
                              │
                       invariant constraints
                              │
                              ▼
                    HENADS / PERSPECTIVES
               competing partial decompositions
                    /        |         \
                   ▼         ▼          ▼
              INTELLECTUAL STRUCTURES
              models / forms / schemas
                    │
                 procession
                    ▼
                  SOUL
        planners / hypotheses / policies
                    │
                    ▼
             LOWER SPECIALISTS
       retrieval / coding / tools / simulation
                    │
                    ▼
                 WORLD
          observations + test results
                    │
                 reversion
                    ▼
          evidence / error / confidence
                    │
                    └──────────► upward revision
```

Translations (binding — prose without these is rejected):
participation = constraint sharing; procession = top-down decomposition;
reversion = evidence/error propagation upward; remaining = invariant preservation;
henads = simultaneously valid specialized perspectives; hierarchy = abstraction depth;
causation = explicit dependency graph; dialectic = hypothesis branching/elimination;
unity = compression/coherence across independent modules.

Surround:
knowledge-base-organism (epistemic graph) + Alethiea (proof obligations) +
QDW (verification ladder) + Cogym (controlled experiments) +
MemoryProof (memory controls) + seed0 (tournament).
Stronger than a Proclus-flavored system prompt.
