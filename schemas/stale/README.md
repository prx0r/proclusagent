# schemas/stale/ — candidates, not moves: NOTHING MOVED this turn

Alpha: verse-claim.json (ledger discipline) + arch-packet.json (mine discipline) are both
live and referenced by SYNTHESIS/KERNELS/MINE-SPEC. Staleness risk is divergence between
the two truth_condition wordings — converge on arch-packet's falsification phrasing when
touched (additive field, never rename). No moves, no renames this turn.
